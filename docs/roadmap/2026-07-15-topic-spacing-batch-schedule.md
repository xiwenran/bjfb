> 原文快照：下沉自 docs/roadmap.md（2026-07-21 迁移），文中状态记号为历史残留

## ✅ 2026-07-15 — 同主题间隔与批量分散排期

- **目标**：只对功能上线后新增记录生效；同账号任意相邻笔记必须严格大于 6 小时（分钟精度最小 361 分钟），同店铺组跨账号相同具体主题在生成排期前一次性确认，批量时间在用户窗口内分层随机且全批分钟唯一。
- **范围**：新增 `topic-index.json` 作为新旧记录边界；排期同时检查当前批、上线后已排期记录和可关联发布历史的已发布记录；旧记录不回填、不推断、不迁移。
- **失败边界**：索引损坏／版本不支持／读取失败、飞书或发布历史读取失败、唯一分钟或 361 分钟容量不足时明确停止；不得少排、静默改日期、压缩间隔或把索引错误当成无冲突。
- **施工文件**：`src/topic-spacing-guard.js`、`src/config-store.js`、`src/server.js`、`src/scheduler-allocator.js`、`scripts/skill_upload.py`、三份针对性测试、两条 Skill 文档、`README.md`。
- **验收**：针对性测试保留 RED／GREEN 证据；全量测试隔离既有账号映射失败；不执行真实上传或公开发布；发布防误逻辑完成后必须独立冷眼审查。
- **状态**：✅ 已完成（commit `f0f291a`；验证：相关测试 32/32、Python/Node 语法通过；全量 79/80，1 项既有账号映射失败）

### 后续由用户处理

- [ ] 用下一批真实笔记完成一次排期与发布观察，确认提醒文案、账号分布和实际发布时间符合使用习惯。
- [ ] 观察期结束后，再决定是否补做修复后的第二次独立冷眼复核；当前 4 个必要边界回归测试为 34/34 通过。
- [ ] 另行处理既有 `tests/account-mapping.test.js` 账号映射基线失败；该问题不属于本次排期改动。
