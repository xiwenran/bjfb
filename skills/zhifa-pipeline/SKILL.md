---
name: zhifa-pipeline
description: PPT一键发布全链路：从PPT文件到飞书多维表格发布记录的全自动化流水线。触发词：批量制作笔记、PPT一键发布、PPT做成笔记发布、PPT转笔记上传、全链路发布、做完直接发、一次做完发布、做好发到小红书、做好发到抖音、PPT做成笔记发抖音、PPT做成笔记发小红书、全流程发布、家长会笔记制作、家长会笔记发布、批量发布笔记、课件转笔记、笔记批量上传。
---

# zhifa-pipeline — PPT → 融景合成 → 飞书建档 全链路 Skill

从 PPT 文件夹开始，全自动完成：PPT 导出图片 → 融景合成笔记图 → 封面自动放置 → 调度矩阵生成 → 标题标签生成（用户确认） → 上传飞书建档 → 已安排/未安排归档 → 上传后回执核验。

**发布状态统一设「待处理」**：上传时发布状态留空（知发设计），后处理统一设为「待处理」。用户在飞书检查确认后手动改为「待发布」触发调度。

## 与公共上传链路的关系（先读这里）

「合成图就绪之后」的全部步骤——扫描、封面、文案、排期、上传前审查、上传、完整性验证、后处理、回执核验、归档、报告——是本 skill 与 `zhifa-upload` **共用的同一套规则**，唯一规范正文在：

```
~/zhifa/skills/_upload-chain/00-总纲.md   （步骤索引 U1–U11 + 全局硬规则 + 共享命令 + 失败处理）
~/zhifa/skills/_upload-chain/01-上传前.md  （U1 扫描 · U2 封面 · U3 文案）
~/zhifa/skills/_upload-chain/02-上传.md    （U4 排期+构建records+预检 · U5 上传前审查 · U6 上传 · U7 验证）
~/zhifa/skills/_upload-chain/03-收尾.md    （U8 后处理 · U9 回执核验 · U10 归档 · U11 报告）
```

**本文件只负责 pipeline 独有的两件事**：① 入口契约（CHECKLIST 收集需求）；② `PPT → 合成图` 前置制作（第 1 节）。合成图一旦就绪，立即转入公共链路，**不在本文件复制或另写链路规则**——链路规则改动只改 `_upload-chain/`。

---

## 0. 入口契约（先读这里）

**用户提需求前，复制填写 CHECKLIST：** `~/zhifa/skills/zhifa-pipeline/CHECKLIST-用户提需求模板.md`

必填项：素材根目录、小红书店铺分组账号规则、抖音账号池、每账号每时段几篇、覆盖策略。可选项：时段调整、特殊账号起始日期。

**覆盖策略是必确认项**，不得隐含默认——三选一（`严格覆盖` / `尽量覆盖` / `只保底发布`）的语义与判定见 `_upload-chain/00-总纲.md`「账号与店铺分组」。用户没说时默认**推荐** `只保底发布`，但仍必须显式问一句 `本轮覆盖策略用哪一种？`

**账号加载**：CHECKLIST 未列账号时，按总纲「账号与店铺分组」读 `accounts.json` 并输出候选给用户确认；店铺分组优先级 6 条同样以总纲为准。用户在 CHECKLIST 里已明确写出店铺分组账号规则时以用户为准，跳过自动加载；账号增减后任务完成时顺手更新 `accounts.json`。

**CHECKLIST 未填写 = 不启动流程**（严格执行，不得简化）：

1. 用户说出触发词但**未提供 CHECKLIST 信息**时，Claude 必须**立即把 CHECKLIST 模板正文完整贴出来**给用户复制填空——不要只回一句「请先填 CHECKLIST」：

   ```bash
   cat ~/zhifa/skills/zhifa-pipeline/CHECKLIST-用户提需求模板.md
   ```

   把 cat 出来的内容直接放在回复里，告诉用户「请把这份模板复制下来，把空格处填好发回给我，我再启动流程。」
2. 不要替用户猜测任何必填项（即便能从最近会话推断），等用户明确填好回复
3. 5 项里只有「时段调整」和「特殊账号起始日期」是可选——其他 3 项缺一不可
4. 用户填好回复后才启动流程

### 中途介入模式（用户手动做了一部分后接入）

用户可能已完成前几步（合成图已有、只需重传等）。识别当前进度对应哪个 U，从该 U 开始，往后把剩余步骤走完——**收尾必回**规则见 `_upload-chain/00-总纲.md`。

| 用户说 | 从哪步开始 | 后续必做 |
|--------|-----------|---------|
| 「重新上传」「重传失败的」 | U6（上传） | U7 → U8 → U9 → U10 → U11 |
| 「模板分配重做」「排期重做」 | U4（排期） | U4 → U5 → … → U11 |
| 「标题改一下」 | U3（文案） | U3 → U4 → … → U11 |
| 「归档一下」 | U10（归档） | U10 → U11 |
| 「检查一下有没有重复」 | U7（验证） | U7 → 报告结果 |

**允许用户拒绝某步**：用户说「不用归档」「跳过审查」是合法的，但 AI 不能自己默认跳过。特别是 U5 上传前审查、U7 验证、U9 回执核验，即使中途介入也要回到公共链路走完。

### 禁止事项（CRITICAL）

- ❌ **不得再写 `/tmp/*.py` 或 `/tmp/*.js` 胶水脚本**：所有步骤已接口化到共享执行层（`_upload-chain/00-总纲.md`「共享执行层命令」），无需临时脚本
- ❌ 不得绕过共享执行层直接调用 `rongjing/cli.py`、`pipeline.py`、`skill_upload.py`，除非按文档规定的参数格式
- ❌ 不得从工具级 skill（zhifa-upload、rongjing）切入全链路任务——本 skill 是全链路入口
- ❌ **完成上传后不得跳过 U7–U11**——即使是中途介入，收尾步骤必须走完（用户明确说跳过除外）

---

## 1. pipeline 独有前置：PPT → 合成图

### 1.1 读 CHECKLIST + 扫描主题列表

1. 读取用户填写的 CHECKLIST 5 项
2. `ls` 素材根目录，自动发现主题子文件夹列表（无需用户单独提供）
3. 列出识别到的 PPT 文件和主题，等用户确认数量正确后继续

```bash
ls "<素材根目录>/笔记制作/"
```

**错误处理**：路径不存在 → 停下告知用户，不静默跳过。

### 1.2 PPT 导出图片 + 融景合成 + 封面放置

**分两步**（`pipeline.py` 不支持 `--cover-source`，要带封面必须拆开走）：

第一步 PPT → 图片：

```bash
python3 ~/ppt-batch-tool/cli.py convert \
  --input "<PPT 所在文件夹>" \
  --output "<输出目录>/PPT图片" \
  [--only-file "<单个 PPT 文件名>"] \
  --max-slides 17
```

第二步 融景合成 + 封面放置（对每个 PPT 导出的图片子文件夹各跑一次）：

```bash
python3 ~/rongjing/cli.py process \
  --input "<输出目录>/PPT图片/<PPT名>" \
  --templates 1 2 3 ... \
  --output "<输出目录>/合成图/<PPT名>" \
  --format JPEG \
  --cover-source "<封面源目录>"
```

- `--cover-source`：rongjing 自动识别两种模式——目录下直接有 `0(1).jpg/0(2).jpg/0(3).jpg` → 复制到所有模板子目录；目录下有多个主题子目录 → 按名称前 6 字符匹配。复制时重命名为 `0.jpg/0(1).jpg/0(2).jpg`
- 模板必须显式列出（`rongjing/cli.py process` 的 `--templates` 是必填项）；先 `python3 ~/rongjing/cli.py list-templates` 取实际可用列表，**模板数以实查为准，不要照搬文档里的数字**
- 用户说「随机 N 个模板」时：从 list-templates 结果随机采样 N 个 name

**输出结构**：

```
合成图/
  <主题A>/
    1/       ← 模板 1
      0.jpg  ← 封面（rongjing 自动放置）
      1.jpg  ← 内容图
    2/       ← 模板 2
  <主题B>/
```

等待完成（约 30 分钟/7 个 PPT），确认 `合成图/` 各主题下有子文件夹且封面 `0.jpg` 存在后继续。

**错误处理**：命令失败 → 报告完整错误信息给用户，不重试。

**pipeline 默认值**：未指定时段时用默认 6:20-7:30 / 13:00-13:30 / 19:00-19:30 / 21:00-22:00；融景模板默认全部可用（不传 `--templates` 给融景则全用，但命令行的 `--templates` 必填，用 list-templates 全量）；平台搭配、模板分散、标题字数/标签数量等链路默认值以 `_upload-chain/` 为准。

---

## 2. 进入公共上传链路（U1–U11）

`合成图/` 就绪后，**Read `~/zhifa/skills/_upload-chain/00-总纲.md`，按 U1–U11 执行**：U1 扫描 → U2 封面 → U3 文案（必停点①）→ U4 排期+构建records+预检（必停点②）→ U5 上传前冷眼审查 → U6 上传 → U7 完整性验证 → U8 后处理 → U9 回执核验 → U10 归档 → U11 报告。

调度矩阵、模板分散、账号排期一律通过共享执行层生成，不允许主会话用 Python/Node heredoc 临时手写分配。

---

## 3. 反面教训（2026-05-07 整改记录）

**事故经过**：56 篇笔记任务，从工具级 skill `rongjing` 切入，把封面图 `0(1).jpg` 误判为内容图传入融景合成，56 篇全部报废。此后补救写了 5 个 `/tmp` 胶水脚本，又触发 API timeout，产生 14 条飞书重复记录。全程纠错超过 4 小时。

**根因**：没走正确入口（`zhifa-pipeline`），而是从中间工具切入，导致对「0 开头 = 封面，1 开头 = 内容图」的约定不知情，脚本满天飞，状态对账失控。

**整改后**：封面放置内建到 `rongjing/cli.py process --cover-source`；调度矩阵内建到 `schedule`；归档内建到 `archive`；上传自适应分批内建到 `skill_upload.py`。**结论：进对入口，跨项目盲区根本不存在。** 详细复盘见 `~/Obsidian/PersonalWiki/项目/知发/changelog/2026-05-07.md`。

### 事故二（2026-05-09）：模板分配 + 飞书限流 + 验证困难

**事故经过**：98 篇笔记（14 主题 × 7 账号），调度矩阵手写时每账号只分配 1 个模板，导致同一账号 14 篇全部用同一模板，视觉雷同。用户删库重传，又撞飞书图片上传限流（98 条从第 15 批开始大量失败，最终 47 成功 / 46 失败），3 轮断点续传才补齐。验证阶段 `/api/records` 反复超时，改用本地导入日志确认到位。

**踩坑点与修复**：① 模板分配「不重复」粒度是跨主题非跨账号 → 共享 `schedule` 校验；② 飞书限流 → 按图片密度分批策略表（见 `02-上传.md` U6）；③ `/api/records` 100+ 条不可用 → `verify` 子命令 / 本地导入日志（见 U7）；④ 重试效率 → 断点续传只传失败 noteKey；⑤ **子代理没用**：中途脱离流程就忘了回来 → 即使中间介入，完成后也要回到公共链路走完 U5/U9 审查节点。

---

## 4. 与其他 Skill 的关系

本 skill 是**自包含的全链路入口**，内部串联 `ppt-batch-tool`（PPT 导出）、`rongjing`（融景合成 + 封面）、共享执行层 `skill_upload.py`（上传链路底层）。

| 场景 | 用哪个 |
|---|---|
| 已有合成图，只需上传 | `zhifa-upload` |
| 只需跑融景合成，不上传 | `rongjing` |
| 只需 PPT → 图片，不合成 | `ppt-batch-tool` |
| 从 PPT 开始，全部做完发布 | **本 skill（zhifa-pipeline）** |
